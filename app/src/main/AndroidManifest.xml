<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.narlocks.delivery_service_app.activity">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/ExternalTheme">
        <activity android:name=".LoginActivity">
        <!--<activity android:name=".NewServiceProviderServicesActivity">-->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".UserSelectionActivity" />
        <activity android:name=".NewClientMainActivity" />
        <activity android:name=".NewClientProfileActivity" />
        <activity android:name=".NewServiceProviderMainActivity" />
        <activity android:name=".NewServiceProviderProfileActivity" />
        <activity android:name=".NewServiceProviderServicesActivity"></activity>
        <!--<activity android:name=".LoginActivity"></activity>-->
    </application>

</manifest>